package ud5.practicas.ExamenTest.ConInterfaces;

public interface PreguntaTest {
    boolean corregir(int opcion);
    String getEnunciado();
    String toString(); // opcional, si se desea un formato específico
}

